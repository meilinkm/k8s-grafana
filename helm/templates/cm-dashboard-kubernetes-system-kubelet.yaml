apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-dashboard-kubernetes-system-kubelet
  labels:
    grafana_dashboard: "1"
data:
  dashboard.json: |
{{ .Files.Get "files/dashboards/dashboard-kubernetes-system-kubelet.json" | indent 4 }}
