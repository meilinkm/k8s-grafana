apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-dashboard-kubernetes-api-server
  labels:
    grafana_dashboard: "1"
data:
  dashboard.json: |
{{ .Files.Get "files/dashboards/dashboard-kubernetes-api-server.json" | indent 4 }}
