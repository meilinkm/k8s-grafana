apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-dashboard-kubernetes-system-kube-scheduler
  labels:
    grafana_dashboard: "1"
data:
  dashboard.json: |
{{ .Files.Get "files/dashboards/dashboard-kubernetes-system-kube-scheduler.json" | indent 4 }}
