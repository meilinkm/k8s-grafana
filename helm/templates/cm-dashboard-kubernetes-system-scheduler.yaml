apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-dashboard-kubernetes-system-scheduler
  labels:
    grafana_dashboard: "1"
data:
  dashboard.json: |
{{ .Files.Get "files/dashboards/dashboard-kubernetes-system-scheduler.json" | indent 4 }}
